2. 자동 주입 설정에서 byName 옵션은 어떤 기능을 수행하나요?
   a. 동일한 타입의 객체를 구분하여 할당합니다.
   b. 생성자의 타입을 기준으로 객체를 할당합니다.
   c. 객체를 자동으로 연관시킵니다.
   d. 특정 메서드의 property를 기준으로 객체를 할당합니다.

3. @ComponentScan 어노테이션을 사용하는 목적은 무엇인가요?
   a. 모든 클래스를 객체로 만들어 메모리에 로딩합니다.
   b. 스프링에서 사용되는 특정 클래스만 객체로 만들어 메모리에 로딩합니다.
   c. 특정 패키지에 @Component로 선언된 클래스만 객체로 만들어 메모리에 로딩합니다.
   d. 특정 패키지의 모든 객체들을 메모리에 로딩합니다.

4. @Component 어노테이션이 사용되는 클래스는 어떤 역할을 수행하나요?
   a. 컨트롤러 역할을 수행합니다.
   b. 서비스 역할을 수행합니다.
   c. 레포지토리 역할을 수행합니다.
   d. 객체로 인식하여 사용합니다.

5. 의존성 자동 주입에서 byType 옵션은 어떤 기능을 수행하나요?
   a. 동일한 타입의 객체를 구분하여 할당합니다.
   b. 생성자의 타입을 기준으로 객체를 할당합니다.
   c. 객체를 자동으로 연관시킵니다.
   d. 특정 메서드의 property를 기준으로 객체를 할당합니다.

7. @Component 어노테이션을 사용하여 객체를 생성하면, 호출 이름은 어떻게 결정되나요?
   a. 클래스명의 첫 글자를 소문자로 변환합니다.
   b. 클래스명의 첫 글자를 대문자로 변환합니다.
   c. 클래스명 그대로 사용합니다.
   d. 사용자가 직접 이름을 설정해야 합니다.

8. @ComponentScan 어노테이션의 base-package 속성에 설정하는 값은 무엇인가요?
   a. 스프링에서 사용되는 특정 패키지 이름입니다.
   b. 모든 클래스가 포함된 패키지 이름입니다.
   c. 특정 패키지 내의 클래스만을 선택하는 정규표현식입니다.
   d. 모든 패키지의 클래스를 로딩합니다.

9. 의존성 자동 주입에서 constructor 옵션은 어떤 기준으로 객체를 할당하나요?
   a. 동일한 타입의 객체를 구분하여 할당합니다.
   b. 생성자의 타입을 기준으로 객체를 할당합니다.
   c. 객체를 자동으로 연관시킵니다.
   d. 특정 메서드의 property를 기준으로 객체를 할당합니다.

10. @ComponentScan 어노테이션에서 include-filter 타입으로 사용되는 옵션은 무엇인가요?
    a. annotation
    b. regex
    c. type
    d. expression

11. @Component 어노테이션이 사용되는 클래스는 어떤 역할을 수행하나요?
    a. 컨트롤러 역할을 수행합니다.
    b. 서비스 역할을 수행합니다.
    c. 레포지토리 역할을 수행합니다.
    d. 객체로 인식하여 사용합니다.

12. 의존성 자동 주입에서 autodetect 옵션은 어떤 기능을 수행하나요?
    a. 동일한 타입의 객체를 구분하여 할당합니다.
    b. 생성자의 타입을 기준으로 객체를 할당합니다.
    c. 객체를 자동으로 연관시킵니다.
    d. 특정 메서드의 property를 기준으로 객체를 할당합니다.

13. @ComponentScan 어노테이션의 base-package 속성에 설정된 패키지 내의 클래스 중에서 특정 클래스를 포함하지 않도록 제외할 때 사용하는 옵션은 무엇인가요?
    a. annotation
    b. regex
    c. type
    d. exclude-filter

14. 의존성 자동 주입에서 byName 옵션은 어떤 기능을 수행하나요?
    a. 동일한 타입의 객체를 구분하여 할당합니다.
    b. 생성자의 타입을 기준으로 객체를 할당합니다.
    c. 객체를 자동으로 연관시킵니다.
    d. 특정 메서드의 property를 기준으로 객체를 할당합니다.

15. @Component 어노테이션이 사용되는 클래스는 어떤 역할을 수행하나요?
    a. 컨트롤러 역할을 수행합니다.
    b. 서비스 역할을 수행합니다.
    c. 레포지토리 역할을 수행합니다.
    d. 객체로 인식하여 사용합니다.

16. 의존성 자동 주입에서 byType 옵션은 어떤 기능을 수행하나요?
    a. 동일한 타입의 객체를 구분하여 할당합니다.
    b. 생성자의 타입을 기준으로 객체를 할당합니다.
    c. 객체를 자동으로 연관시킵니다.
    d. 특정 메서드의 property를 기준으로 객체를 할당합니다.

17. @ComponentScan 어노테이션의 exclude-filter 타입으로 사용되는 옵션은 무엇인가요?
    a. annotation
    b. regex
    c. type
    d. expression

18. @Component 어노테이션이 사용되는 클래스는 어떤 역할을 수행하나요?
    a. 컨트롤러 역할을 수행합니다.
    b. 서비스 역할을 수행합니다.
    c. 레포지토리 역할을 수행합니다.
    d. 객체로 인식하여 사용합니다.

19. 의존성 자동 주입에서 byType 옵션은 어떤 기능을 수행하나요?
    a. 동일한 타입의 객체를 구분하여 할당합니다.
    b. 생성자의 타입을 기준으로 객체를 할당합니다.
    c. 객체를 자동으로 연관시킵니다.
    d. 특정 메서드의 property를 기준으로 객체를 할당합니다.

20. @ComponentScan 어노테이션의 base-package 속성에 설정하는 값은 무엇인가요?
    a. 스프링에서 사용되는 특정 패키지 이름입니다.
    b. 모든 클래스가 포함된 패키지 이름입니다.
    c. 특정 패키지 내의  클래스만을 선택하는 정규표현식입니다.
    d. 모든 패키지의 클래스를 로딩합니다.